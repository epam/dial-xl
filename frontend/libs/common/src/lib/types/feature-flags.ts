// askAI - ai modal which opened on space when inside spreadsheet
// copilotAutocomplete - suggestions which showed when typing something in cell editor, formula bar or code editors

export type FeatureFlag = 'askAI' | 'copilotAutocomplete';
