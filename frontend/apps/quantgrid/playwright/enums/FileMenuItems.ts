export enum FileMenuItems {
  CreateProject = 'Create project',
  OpenProject = 'Open project',
  CreateWorkSheet = 'Create worksheet',
  DeleteProject = 'Delete project',
  CloseProject = 'Back to projects',
  RenameProject = 'Rename project',
}
