export enum CollapseSection {
  Title = 'title',
  ChartType = 'chartType',
  Selectors = 'selectors',
  SizeLocation = 'sizeLocation',
  Series = 'series',
  Data = 'data',
  XAxis = 'xAxis',
  Orientation = 'orientation',
}
